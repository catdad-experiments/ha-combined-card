{
  "name": "ha-combined-card",
  "version": "1.0.0",
  "description": "Home Assistant card that seemlessly combines multiple other cards into one",
  "scripts": {
    "start": "node .esbuild.js --watch --dev",
    "build": "node .esbuild.js --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "custom-card-helpers": "^1.9.0",
    "esbuild": "^0.20.1",
    "lit": "^3.1.2"
  }
}
